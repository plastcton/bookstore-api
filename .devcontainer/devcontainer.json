{
    "name": "bookstore-api",
    "dockerComposeFile": "../compose.yaml",
    "service": "app",
    "workspaceFolder": "/workspace",
    // "postCreateCommand": "bash -c \"python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt\""
}